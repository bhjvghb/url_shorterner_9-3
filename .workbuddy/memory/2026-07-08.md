# 2026-07-08

## 管理面板 Tab 切换修复
- **根因：** CSS 只定义了 `.modal-overlay.hidden`，没有通用 `.hidden` 类，导致 admin_dashboard.html 三个 tab 内容同时显示
- **修复：** style.css 添加 `.hidden { display: none !important; }`，admin_dashboard.html 的 switchTab 改为显式传 el 参数

## 批量缩短升级
- 后端 `/batch_shorten` 改为接受 JSON body `{items: [{url, custom_code, password}], expiry}`
- 每条支持独立自定义短码和密码保护
- 前端新增 URL 自动检测（正则 `https?:\/\/[^\s<>"'\}\]]+`），粘贴→识别→编辑表格→提交
- 结果标注 is_custom / is_protected 标签
- 新增 `batch_detect_btn` 翻译键（三语 218 键一致）

## 代码审计修复
- **reset_password DB 泄漏**：密码验证失败时添加 cur.close/conn.close
- **KeyError 风险**：register/login/forgot_password/reset_password 四路由的 `request.form['key']` 改为 `.get()`，防止 500
- **btn-accent 缺失**：style.css 添加 `.btn-accent` + hover 样式
- **未使用导入**：移除 `from PIL import Image`
- **PWA 文件确认**：manifest.json / icon-192.png / icon-512.png / sw.js 均存在
- **_() 函数安全性**：在路由中调用正常，依赖 flask.g 的请求上下文
- **潜在性能问题**：geolocate_ip 同步阻塞最多 3 秒（低优，未修）
- **CSRF**：6 个 POST 路由缺少 CSRF 保护（低优，建议后续用 Flask-WTF）
- **SMTP 端口 465**：不支持 SSL 直连模式（低优）
