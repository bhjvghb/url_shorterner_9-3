# 2026-07-02

## 一键分析报告功能

### 后端重构
- 新增 `get_click_analytics(short_code, user_id, username)` 公共函数，提取 link_stats 中的数据分析逻辑，供 stats 和 report 两页面共用
- 新增 `unique_ips`（去重 IP 独立访客）、`first_click_at`、`last_click_at` 指标
- 新增 `GET /report/<short_code>` 路由，需登录 + 权限验证，调用公共分析函数
- `link_stats()` 路由改为调用 `get_click_analytics()`，代码去重

### report.html 报告页 (新建)
- 独立 HTML 报告，使用现有 style.css 保持一致设计
- 报告头：短码、原始 URL、生成时间、时间范围
- 概览卡片：总点击 / 今日 / 独立访客 / 主要国家
- 5 个图表：日趋势折线图、来源环形图、国家/浏览器/OS 水平柱状图
- 点击明细表（按时间排序）
- 「下载 HTML」按钮（Blob 下载）和「打印」按钮（window.print）
- @media print：隐藏操作栏和导航，图表 break-inside: avoid
- 响应式：移动端 2 列卡片、单列图表

### dashboard.html
- Actions 列新增 Report 按钮（btn-accent），链接到 /report/<short_code>

### 三语翻译 +9 键
en/zh-CN/zh-HK 各 207 键，新增：generate_report / click_report / unique_visitors / first_click / last_click / generated_at / period / download_report / print_report

### UI 品牌更新（用户自行完成）
- 品牌名改为 LinkSnap，intro改为 HKU ECE 主题
- 全局 style.css 替代内联样式
- dashboard 按钮改为 btn-xs，新增 toast 提示

涉及文件：app.py (+110行), report.html (新建240行), dashboard.html (+1行), en.json/zh-CN.json/zh-HK.json (+9键)
