# 2026-07-07

## PWA 支持 + 图表字体放大 + 报告 PDF 导出

### PWA 基础设施（方案 A：改动最少）
- 新建 `static/manifest.json`：应用名称 LinkSnap、主题色 #6366f1、192+512px 图标
- 新建 `static/sw.js`：Service Worker，网络优先策略，缓存静态资源
- 新建 `static/icon-192.png` 和 `static/icon-512.png`：PIL 生成紫色圆角 + 白色 L 字母图标
- 新建 `templates/_pwa_head.html`：manifest 链接、theme-color、Apple PWA 元标签
- 新建 `templates/_pwa_script.html`：service worker 注册脚本

### 模板 PWA 嵌入（12 个模板全部更新）
通过 sed 批处理在所有模板的 `<head>` 和 `</body>` 中插入 PWA 组件 include：
index / dashboard / link_stats / report / login / register / forgot_password / reset_password / protected / success / error / admin_dashboard

### 图表轴标签字体放大
- link_stats.html：12→14, 13→15
- report.html：10→14

### 报告 PDF 导出替代 HTML
- report.html 引入 html2pdf.js CDN（0.10.3）
- 按钮从「下载 HTML」改为「下载 PDF」
- downloadReport() 重写为 downloadPDF()：通过 html2pdf 将 .report-container 转为 PDF 并下载
- 三语翻译新增 download_pdf 键（en/zh-CN/zh-HK 均为 209 键）

### 批量缩短功能
- 新增 POST /batch_shorten 路由：解析多行 URL，逐个生成短码并写入数据库+生成二维码，返回 JSON 结果
- index.html 增加 Single / Batch 模式切换标签，Batch 模式下显示 textarea + 过期选择器
- 异步提交 → 结果模态框，每项显示 ✔ / ✘ 状态，成功项可复制短链接，底部汇总成功/失败数
- 新增 8 个翻译键：single_mode / batch_mode / batch_placeholder / batch_shorten_btn / batch_results / batch_summary / success_count / fail_count
- 三语翻译均为 217 键一致

### app.py 去重修复
删除重复的旧 link_stats 路由（导致 Render 部署失败 AssertionError: overwriting endpoint）

涉及文件：manifest.json(新建), sw.js(新建), icon-192.png(新建), icon-512.png(新建), _pwa_head.html(新建), _pwa_script.html(新建), index.html, dashboard.html, link_stats.html, report.html, login.html, register.html, forgot_password.html, reset_password.html, protected.html, success.html, error.html, admin_dashboard.html, en.json, zh-CN.json, zh-HK.json, app.py
